<div class="container">
  <% @bookings.each do |booking| %>
    <%= booking.treehouse.name %>
    <br>
    <%= booking.start_date %>
    <br>
    <%= booking.end_date %>
    <br>
    <%= (booking.end_date - booking.start_date) %>
  <% end %>
</div>
